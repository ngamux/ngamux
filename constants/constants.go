package constants

const (
	// NotFoundMessage static string contains `not found`
	NotFoundMessage = `not found`
	// MethodNotAllowedMessage static string contains `method not allowed`
	MethodNotAllowedMessage = `method not allowed`
)

const (
	// ContextKeyError get context value with `error` key
	ContextKeyError = `error`
)
